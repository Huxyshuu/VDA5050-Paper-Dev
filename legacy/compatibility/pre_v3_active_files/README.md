# Pre-v3 compatibility files

These files were removed from the active runtime tree because their headers or
edge fields use VDA 5050 2.x conventions even when a filename suggested v3.
They are retained only for historical comparison and must not be published to
`vda5050/v3/...` topics.
