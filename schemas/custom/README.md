# Custom schemas and usage-profile validation

Place project-specific validation schemas here. Do not modify the copied official VDA 5050 files in `../vda5050_v3/`.

Crane/warehouse semantics should preferably remain schema-valid VDA messages plus documented actions, factsheet declarations and usage-profile rules. Add a custom schema only when an additional project file format genuinely needs machine validation.
