% Include-ready research-design bookmarks for the ICIEA 2027 working manuscript.
% Keep these as comments until the frozen physical campaign produces results.

\section{Experimental Methodology}
\label{sec:methodology}

\subsection{Research Questions and Hypotheses}
\label{subsec:research_questions}
% RQ1: Incremental dispatch and acknowledgement latency of VDA over native.
% RQ2: Operational effect on representative physical action duration.
% RQ3: Preservation of task-level command, state, and outcome behaviour.
% H1: VDA adds positive interface latency.
% H2: VDA overhead is small relative to physical motion duration.
% H3: Both paths reach functionally equivalent physical endpoints.

\subsection{Latency Definitions and Instrumentation}
\label{subsec:timing}
% Source of truth: benchmark/event_schema.json and immutable host JSONL files.
% COMMAND_ISSUED -> MQTT_RECEIVED -> VDA_ACCEPTED -> NATIVE_DISPATCH
% -> NATIVE_ACK -> MOTION_STARTED -> MOTION_COMPLETED -> RESULT_OBSERVED.
% Native trials omit the two VDA-only events.
% Durations use time.monotonic_ns() only. UTC is descriptive metadata.
% Never subtract monotonic timestamps from different host/boot_id pairs.

\subsection{ROX-Diff Rotation Benchmark}
\label{subsec:rox_benchmark}
% Same x/y and headings separated by pi/2. Native and VDA trials invoke the
% identical NavigateToPose goal. Report paired A->B or B->A differences.

\subsection{Overhead-Crane Hoist Benchmark}
\label{subsec:crane_benchmark}
% Two supervised fixed hoist heights. Both paths use crane.set_target_hoist()
% and move_hoist_to_target(fast=True); VDA adds MQTT/schema/adapter processing.

\subsection{Experimental Procedure}
\label{subsec:procedure}
% Five paired pilot trials per device, then freeze. Main campaign: 30 pairs per
% device with randomized Native/VDA treatment order and excluded reset rows.
% Record the exact Git commit, config hashes, ROS/RMW/Nav2, broker/QoS, crane
% software, and network topology using benchmark/freeze_environment.py.

\subsection{Data Analysis}
\label{subsec:analysis}
% Integrity gate: all required events on one host/boot clock.
% Report n, median, IQR, p95, p99, paired VDA-minus-native median, bootstrap
% 95 percent CI, percentage overhead, and trial accounting.

\section{Results}
\label{sec:results}

\subsection{ROX-Diff Command Latency}
\label{subsec:rox_results}
% RESULT BOOKMARK: analysis/output/latency_summary.csv and latency_comparison.pdf

\subsection{Overhead-Crane Command Latency}
\label{subsec:crane_results}
% RESULT BOOKMARK: analysis/output/latency_summary.csv and latency_comparison.pdf

\subsection{Incremental VDA 5050 Overhead}
\label{subsec:overhead_results}
% RESULT BOOKMARK: analysis/output/paired_differences.csv and
% latency_summary_table.tex. Keep devices separate; do not rank ROS 2 vs OPC UA.

\subsection{Task-Level Functional Behaviour}
\label{subsec:functional_results}
% RESULT BOOKMARK: analysis/output/trial_accounting.csv plus 5--10 descriptive
% complete VDA handovers after the confirmatory command campaign.
